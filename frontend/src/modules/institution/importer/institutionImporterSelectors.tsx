import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const institutionImporterSelectors = importerSelectors(
  'institution.importer',
);

export default institutionImporterSelectors;
